---
title: "Creating trades"
slug: "/guides-new/trades"
keywords: [imx-traders, imx-dx]
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

Creating a trade is also known as filling an order. A user executes a trade by agreeing to purchase an asset at the terms specified in the order and then becomes the new owner of the asset.

### Guides:
* [Create trades using the Core SDK](#create-trades-using-the-core-sdk)
* [Create trades on StarkNet](#create-trades-on-starknet)

## Create trades using the Core SDK

### 1. Initialize the Core SDK
In order to use the Core SDK, you need to [initialize it](./get-started#getting-started-with-the-core-sdk).

### 2. Generate signers
Creating an trade for a user requires a user's signature, so your application will need to create signers. See the guide on [how to generate signers](/docs/guides-new/generate-signers).

### 3. Create the trade

Filling the order with ID 7232.

<Tabs>
  <TabItem value="typescript" label="Typescript Core SDK">

Sdk reference: [createTradeRequestV1](https://docs.x.immutable.com/sdk-references/core-sdk-ts/1.0.0-beta.2/interfaces/index.createtraderequestv1), [GetSignableTradeRequest](https://docs.x.immutable.com/sdk-references/core-sdk-ts/1.0.0-beta.2/interfaces/index.getsignabletraderequest)

#### Request

```ts

const createTrade = async (wallet: WalletConnection, orderId: number) => {

    const ethAddress = await wallet.ethSigner.getAddress();
    const tradeData = {
      order_id: orderId,
      user: ethAddress
    } as GetSignableTradeRequest;

    const response = await client.createTrade(wallet, tradeData);
    return response;
  }

createTrade(wallet, 7232)
  .then((result) => {
    console.log(result)
  })
  .catch((e) => {
    console.log(e)
  })

```

#### Example response


```ts

{ 
  trade_id: 226892, // ID of trade within Immutable X
  status: '' // Current status of trade
}


```

  </TabItem>
  <TabItem value="kotlin" label="Kotlin (JVM) Core SDK">
    Add description, if required.
    <br/><br/>

```kotlin
// Add code
``` 
  </TabItem>
  <TabItem value="Swift" label="Swift Core SDK">
    Add description, if required.
    <br/><br/>

```swift
// Add code
```
  </TabItem>
  <TabItem value="go" label="Golang Core SDK">
    Add description, if required.
    <br/><br/>

```go
// Add code
```
  </TabItem>
</Tabs>


## Create trades on StarkNet





